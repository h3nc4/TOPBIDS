import React from 'react';
import { Text } from 'react-native';

const Header: React.FC = () => {
    return (<Text style={{ fontSize: 24, fontWeight: 'bold', textAlign: 'center', marginBottom: 10 }}>TOPBIDS</Text>);
};

export default Header;
